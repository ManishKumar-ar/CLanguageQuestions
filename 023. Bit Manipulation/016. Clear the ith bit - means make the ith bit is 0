#include <iostream>
using namespace std;

int clearBit(int n, int i) {
    int mask = ~(1 << i);
    int result = n & mask;
    return result;
}

int main() {
    int n = 13;
    int result = clearBit(n, 2);
    cout << "Value after the bit is cleared: " << result << endl;
    return 0;
}


//output
//Value after the bit is clear : 9



//Clear
//"Clearing" a bit means setting it to 0 regardless of its current state:

//If the bit is 1, it becomes 0.
//If the bit is already 0, it remains 0.


Toggle
"Toggling" a bit means flipping its state:

If the bit is 0, it becomes 1.
If the bit is 1, it becomes 0.





