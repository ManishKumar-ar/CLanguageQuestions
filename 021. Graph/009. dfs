#include <iostream>
#include <vector>
using namespace std;

void dfs(vector<int> graph[11], int node, int vis[11])
{
    vis[node] = 1;

    cout << node << " ";

    for (int i = 0; i < graph[node].size(); i++)
    {
        if (!vis[graph[node][i]])
        {
            dfs(graph, graph[node][i], vis);
        }
    }
}

int main()
{
    vector<int> graph[11];

    graph[10] = {6,9};
    graph[9] = {8, 7, 10};
    graph[6] = {5, 4, 10};

    int vis[11] = {0}; // Initialize visited array

    int node = 10;
    dfs(graph, node, vis);

    return 0;
}

//outout
// 10 6 5 4 9 8 7 
