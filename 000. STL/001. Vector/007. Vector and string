#include <iostream>
#include <vector>
#include <array>
using namespace std;

void printVecotor(vector<string> &v)
{
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
  
    cout << endl;
}

int main()
{
    vector<string> v;

    int n;
    cin>>n;

    for(int i=0;i<n;i++)
    {
        string x;
        cin>>x;
        v.push_back(x);
    }

    printVecotor(v);
}

// output
// 5
// hello
// manish
// vishal
// kajal
// reema
// hello manish vishal kajal reema 
