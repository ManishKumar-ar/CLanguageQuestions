#include <iostream>
#include <array>

using namespace std;

int digit(int n)
{
    if (n == 0)
        return 0;

    return digit(n / 10) + n % 10;
}
int main()
{
    int x = 7854;
    int result = digit(x);
    cout << result;
}

//output 
// 24
