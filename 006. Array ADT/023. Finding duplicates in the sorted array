#include<iostream>
using namespace std;
int main()
{
    int A[10]={3,6,8,8,10,15,15,20,20};
   int lastduplicate=0;

   for(int i=0;i<9;i++)
   {
    if(A[i]==A[i+1]&&A[i]!=lastduplicate)
    {
        cout<<A[i]<<" ";
        lastduplicate=A[i];
    }
   }
}

//output 
//8 15 20 

